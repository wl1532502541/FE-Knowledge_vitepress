import { LocaleConfig, SiteData } from '../../types/shared';
export type { SiteData, PageData, HeadConfig, LocaleConfig, Header } from '../../types/shared';
export declare const EXTERNAL_URL_RE: RegExp;
export declare const inBrowser: boolean;
export declare function createLangDictionary(siteData: {
    themeConfig?: any;
    locales?: Record<string, LocaleConfig>;
}): Record<string, {
    lang: string;
    label: string;
}>;
export declare function resolveSiteDataByRoute(siteData: SiteData, route: string): SiteData;
